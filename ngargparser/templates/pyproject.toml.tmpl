[project]
name = "{TOOL_NAME}"
version = "0.1.0"
description = "TODO: short description for {TOOL_NAME}"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "python-dotenv",
]

[dependency-groups]
test = ["pytest"]

[tool.ngargparser]
scaffold_version = "{NGARGPARSER_VERSION}"
